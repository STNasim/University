\usepackage[spanish]{babel} % gestiona idiomas
%\usepackage{geometry}
% \geometry{top=1.5cm, bottom=1.0cm, left=1.0cm, right=1.0cm}

% \usepackage[a4paper]{geometry} % es para definir los margenes
\usepackage[a4paper,top=19mm,bottom=43mm,left=13mm,right=13mm,marginparwidth=1.75cm]{geometry}

%https://www.ieee.org/content/dam/ieee-org/ieee/web/org/pubs/format-definition-table-and-glossary.pdf

\usepackage[colorlinks=true, allcolors=blue]{hyperref} %  Permite crear links en distintas partes del documento y también links a paginas web

\usepackage[utf8]{inputenc}% permite gestionar la escriture de caracteres que no estan considerados en el ingles.

\usepackage{fancyhdr} % Tiene comando s para modificar el encabezado y pie de pagina

\usepackage{longtable} % es útil para crear tablas que ocupan mas de una página

\usepackage{multicol} % Permite la distribución del texto en columnas

\usepackage{subcaption}% Permite gestiona leyendas o pies de fotos dentro de otro elemento  que tiene a su vez su propia leyenda

\usepackage{verbatim}% introduce a dos entornos
% 1) permite crear un espacio en el cual se puede escribir y aparezca de forma literal en el texto, es decir los comandos no son ejecutados aquí
%2) introduce un entorno "comment" que permite escribir comentarios multilínea

%\usepackage{xcolor}% Permite cambiar el color de un texto, resaltarlo de un color determinado, crear bordes a su alrededor

\usepackage{amsmath}

\usepackage{graphicx}

\usepackage{listings}

\usepackage{import}

\usepackage[table,xcdraw]{xcolor}

\usepackage{float}